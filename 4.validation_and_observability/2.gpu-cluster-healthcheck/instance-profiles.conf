# GPU Cluster Instance Profiles
# Defines expected hardware configuration per EC2 instance type.
# Used by health check scripts to validate detected hardware counts.
#
# Format: INSTANCE_TYPE|GPU_COUNT|EFA_COUNT|NVLINK_EXPECTED|EFA_PROVIDER
#
# GPU_COUNT:        Number of GPUs (NVIDIA)
# EFA_COUNT:        Number of EFA network interfaces
# NVLINK_EXPECTED:  Whether NVLink interconnect is expected (true/false)
# EFA_PROVIDER:     Libfabric provider name

# ─── NVIDIA GPU Instances ────────────────────────────────────────────────────
p4d.24xlarge|8|4|true|efa
p4de.24xlarge|8|4|true|efa
p5.48xlarge|8|32|true|efa
p5e.48xlarge|8|32|true|efa
p5en.48xlarge|8|16|true|efa
p6-b200.48xlarge|8|8|true|efa