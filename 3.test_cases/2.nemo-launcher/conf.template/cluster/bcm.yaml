partition: null
account: null
exclusive: True
gpus_per_task: null
gpus_per_node: 8
mem: 0
job_name_prefix: "nemo-megatron-"
#gres: "gpu:8"     # Uncomment for Slurm clusters that use gres to allocate GPU instances.
srun_args:
  - "--no-container-mount-home"
  - "-l"
  - "--open-mode=append"
#stderr_to_stdout: True  # Uncomment to redirect job's stderr to stdout.
